<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches the launcher's own widget corner radius on API 31+, so the card doesn't look
         out of step with every other widget on the home screen. -->
    <dimen name="widget_corner_radius">@android:dimen/system_app_widget_background_radius</dimen>
</resources>
